extends base/pug/main

append variables
  - var title = 'Контакты'
  - var pageClass = 'contacts-page'
  - var baseDir = '../static';

block content
  include components/breadcrumbs/breadcrumbs
  include components/section/contacts/contacts
